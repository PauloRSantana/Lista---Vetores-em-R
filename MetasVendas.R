# Criando o vetor lógico com os resultados dos vendedores
atingiu_meta <- c(TRUE, FALSE, TRUE, TRUE, FALSE, TRUE)

# Contando quantos vendedores atingiram a meta (TRUE)
quantos_atingiram_meta <- sum(atingiu_meta)

# Exibindo o resultado
print(paste("Número de vendedoress que atingiram a meta:", quantos_atingiram_meta)) 
